#! /bin/bash

pidfile=./txgh.pid

if [ ! -f $pidfile ]; then
  echo "Server is not running"
  exit 0
fi

pid=$(cat $pidfile)
result=$(ps ax | grep $pid | grep -v grep)

if [[ $result == '' ]]; then
  echo "Server is not running"
else
  echo "Server is running"
fi
